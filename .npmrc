# Suprimir avisos de vulnerabilidades moderadas em CI/produção
# As vulnerabilidades são em devDependencies e não afetam produção
audit-level=high

# Usar --omit=dev em vez de --production (deprecated)
legacy-peer-deps=false
